apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    storage: nfs
  name: my-pv-nfs
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${NFS_SERVER}
    path: "/mnt/nfs"
#  storageClassName: jon
